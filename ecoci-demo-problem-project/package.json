{
  "name": "ecoci-demo-problem-project",
  "version": "1.0.0",
  "description": "Intentional CI/CD and code issues for EcoCI demo",
  "main": "src/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/",
    "test:unit": "jest src/index.test.js --runInBand",
    "test:integration": "jest src/index.test.js --runInBand",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
